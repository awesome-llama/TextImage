# save and load images
# TODO add functions here, replace compress_image.py

from PIL import Image
import numpy as np
import layer_A8
import layer_RGB8


def compress_img(image_path, output_path, mode='RGB', lossy_tolerance=0, debug=False):
    pass
    # choose where to load and save, specify colour mode, compression, debug, 